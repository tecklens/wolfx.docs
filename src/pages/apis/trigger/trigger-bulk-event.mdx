import CardEndpoint from "../../../components/CardEndpoint";
import Row from "../../../components/grid/Row";
import Column from "../../../components/grid/Column";
import Variables from "../../../components/Variables";
import Space from "../../../components/grid/Space";
import CodeExample from "../../../components/CodeExample";
import ResponseJson from "../../../components/ResponseJson";

# Trigger Event

Trigger event is the main (and only) way to send notifications to subscribers.
The trigger identifier is used to match the particular workflow associated with it.
Additional information can be passed according the body interface below.

---

<Row>
    <Column>
        <Variables variables={[
            {
                name: 'workflowId',
                type: 'string',
                required: true,
                description: 'The ID of the Workflow you created.'
            },
            {
                name: 'targets',
                type: 'object[]',
                required: true,
                description: 'Information of the message recipient.',
                children: [
                    {
                        name: 'subcriberId',
                        type: 'string',
                        required: true,
                        description: 'We will create a subscriber with the ID you provide.'
                    },
                    {
                        name: 'phone',
                        type: 'string',
                        required: true,
                        description: ''
                    },
                    {
                        name: 'email',
                        type: 'string',
                        required: true,
                        description: ''
                    },
                    {
                        name: '...variables',
                        type: 'any[]',
                        required: false,
                        description: 'The variables you self-define in the workflow.'
                    },
                ]
            },
            {
                name: 'overrides',
                type: 'object',
                required: false,
                description: 'Override the variables we predefined.'
            },
        ]}/>
    </Column>
    <Space/>
    <Column>
        <CardEndpoint url={'/v1/trigger'} method={'POST'}/>
        <ResponseJson title={'REQUEST'} status={null}>
            ```json
            {
                "workflowId": "string",
                "data": {},
                "target": {
                "subcriberId": "string",
                "phone": "string",
                "email": "string"
            },
                "overrides": {}
            }
            ```
        </ResponseJson>
        <br />
        <ResponseJson>
            ```json
            {
                "transactionId": "string"
            }
            ```
        </ResponseJson>
    </Column>
</Row>